{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",  "scripts": {
    "start": "node index.js",
    "dev": "pm2 start ecosystem.config.js --env development",
    "prod": "pm2 start ecosystem.config.js --env production",
    "stop": "pm2 stop xenonepal-server",
    "restart": "pm2 restart xenonepal-server",
    "delete": "pm2 delete xenonepal-server",
    "status": "pm2 status",
    "logs": "pm2 logs xenonepal-server",
    "monitor": "pm2 monit",
    "deploy": "npm run stop; npm run delete; npm run prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "compression": "^1.8.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.19.2",
    "helmet": "^8.1.0",
    "mongoose": "^8.15.1",
    "pm2": "^6.0.8"
  }
}
